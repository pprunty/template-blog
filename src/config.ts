// config.ts
export const SITE_URL = "https://template-blog-xi.vercel.app";

export const AUTHOR = {
  name: "Patrick Prunty",
  url: `${SITE_URL}/about`, // Use SITE_URL to create a full URL to the about page
  publisherName: "Patrick's Blog",
  publisherLogo: `${SITE_URL}/512x512.png` // Use SITE_URL for publisher logo
};

// You can add other reusable constants here, such as default keywords, default sections, etc.
export const DEFAULT_SECTION = "Travel";
